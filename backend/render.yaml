services:
  - type: web
    name: yumix-api
    runtime: node
    rootDir: backend
    buildCommand: npm ci --omit=optional && npm run build && npm prune --omit=dev
    startCommand: npx prisma db push && npm run start:prod
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: NODE_ENV
        value: production
      # Supabase Postgres — set both in Render dashboard (Project Settings → Database → ORM → Prisma)
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: never
      - key: CORS_ORIGIN
        value: https://www.yumix.ge
      - key: RESEND_API_KEY
        sync: false
      - key: EMAIL_FROM
        sync: false
